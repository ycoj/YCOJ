# Training route reference

Registered routes are `/training`, `/training/create`, `/training/:tid`, `/training/:tid/edit`, `/training/:tid/file`, and `/training/:tid/file/:filename`. See [README.md](./README.md) for each decorated parameter, DAG validation, operation body, redirect, file upload, signed download, and permission contract.
