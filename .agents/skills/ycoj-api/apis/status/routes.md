# Status route reference

The package registers `GET /status` (rendered status HTML) and privileged `POST /status/update` (judge heartbeat JSON `{ok:1}`). Complete model types, headers, authentication, and examples are in [README.md](./README.md).
