# Record route reference

Registered HTTP routes are `GET /record` and `GET/POST /record/:rid`; registered connections are WebSocket `/record-conn` and `/record-detail-conn`. Query decorators, operation bodies, exact HTML/JSON/binary responses, filtering permissions, handshake, and event payload examples are in [README.md](./README.md).
